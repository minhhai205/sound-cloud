mixin search-option()
  div.container.mt-3
    .list-group.sidebar-menu(button-search)
      each item in dataSearchOption
        button.btn.btn-light(type="button" data-name=item.dataName class=`${item.class}`)
          i.fa.mr-2(class=`${item.icon}`)
          | #{item.name}